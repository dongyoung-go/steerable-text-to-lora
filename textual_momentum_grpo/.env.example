# Copy this file to .env (gitignored) and fill in your real key.
#   cp .env.example .env
#
# Read by tmgrpo/llm_client.py via python-dotenv. A real shell-exported OPENAI_API_KEY always
# takes precedence over this file -- .env is just a local-dev convenience.
OPENAI_API_KEY=
