cli:
  hf_checkpoint: ${paths.model_dir}/qwen3-8b
  load: ${paths.model_dir}/qwen3-8b-torch-dist
  ref_load: ${paths.model_dir}/qwen3-8b-torch-dist
  save: ${paths.checkpoint_dir}
  save_interval: 50
